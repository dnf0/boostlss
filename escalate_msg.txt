STATUS: NEEDS_CONTEXT

I am blocked on Task 2 due to discrepancies between the provided instructions and the actual state of the codebase:

1. **Missing Incremental API**: The task snippet expects `run_model.initialize(&run_data)?` and `run_model.fit_update(&run_data)?`. However, `BoostLss` does not have an incremental fitting interface. The fitting loop and state (`current_predictions`, `cached_learners`) are deeply encapsulated inside `engine::cyclical::fit_cyclical` and `noncyclical::fit_noncyclical`. Adding these methods to `BoostLss` would require a significant refactor of the engine that goes beyond the plan.
2. **Incorrect Types in Snippet**: The `param_names` implementation in the instructions assumes `self.learners` contains `(String, BaseLearner)` pairs, but it actually contains `(usize, BaseLearner)`.

How should I proceed?
A. Refactor `engine::cyclical` (and `BoostLss`) to expose an incremental fitting iterator/interface so we can step through and early stop.
B. Implement the `q` threshold early stopping logic directly inside `fit_cyclical` (or a dedicated `fit_stabsel` function in the engine).
C. Something else?
